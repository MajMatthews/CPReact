import React from "react";
import ReactDOM from "react-dom";
import {Button} from 'react-toolbox/lib/button';

export default class ComponentExample extends React.Component {
  render() {
  // 	const {doc, i} = this.props;
  // 	let snippet;
  // 	const codeLines = doc.snippet;
  // 			if (doc.snippet !== undefined || null) {
		// 	  snippet = <div className="snippet" dangerouslySetInnerHTML={ {__html: codeLines } }/>
		// };

    return (
      <div className="component-documentation">
      test 2
       
      </div>
    );
  }
};